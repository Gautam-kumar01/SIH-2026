# Mock Search, Upload, and Export QA

The desktop and 390×844 mobile workspace screenshots show the prototype-tools card remains in the dossier column and does not cover the Cesium map. The mock-record search input, All/Mock ULPINs/Ownership filter buttons, and disabled-until-available mock-details PDF action fit within the existing card. The drag-and-drop zone remains visible and touch-usable through its Browse files label.

The mock record list is empty until a user generates a sample ID, which prevents fabricated records from appearing. The PDF export is likewise disabled until a generated mock record exists. Sample files are browser-local and constrained to the documented supported types and 25 MB size limit.

The new flows retain DEMO / NON-AUTHORITATIVE wording and do not change the source-backed map, existing evidence levels, or measurement safeguards.
